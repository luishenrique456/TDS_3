// Implemente uma função que recebe como parâmetro um array de números, calcule
// a média aritmética e retorne o resultado.
const numeros = [3, 7, 3, 2, 7, 10, 2, 15, 10, 20];

function calcular_media(numeros){
    for(let i = 0;i>numeros.length;i++){
        
    }
}


console.log(calcular_media())